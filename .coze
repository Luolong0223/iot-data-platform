[project]
requires = ["nodejs-24"]

[dev]
build = ["pip", "install", "-r", "requirements.txt"]
run = ["python", "run.py"]

[deploy]
build = ["bash", "-c", "pip install -r requirements.txt && python init_mysql.py"]
run = ["sh", "-c", "gunicorn -w 4 -b 0.0.0.0:${DEPLOY_RUN_PORT} app:create_app()"]
